type Slot {
  id: ID! @unique
  name: String!
  number: String!
  time: String!
  date: String!
}

type UnitCode {
  id: ID! @unique
  deviceId: String
  unit: String!
  codes: String!
  createdAt: DateTime!
}
